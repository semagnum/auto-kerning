[project]
name = "auto-kerning"
version = "0.0.1"
description = "Automatic kerning for Blender text objects"
maintainers = [
  {name = "Natasha Brown", email = "natasha@bookflyers.com"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fonttools",
]
